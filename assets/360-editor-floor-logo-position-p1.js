window.__lava360FloorLogoCode=(window.__lava360FloorLogoCode||'')+"(() => {\n  'use strict';\n\n  const VERSION = '20260714-floor-logo-position2';\n  const STORAGE_KEY = 'lava360:floor-logo-position:v2';\n  const MAX_PREVIEW_WIDTH = 1800;\n  const state = Object.assign({\n    x: 50,\n    y: 82,\n    width: 18,\n    heightScale: 72,\n    rotation: 0,\n    opacity: 100,\n    anchor: 'center',\n    panoramaName: 'lava360-floor-logo.jpg'\n  }, readState());\n\n  let modal = null;\n  let panel = null;\n  let canvas = null;\n  let ctx = null;\n  let panoramaImage = null;\n  let logoImage = null;\n  let panoramaObjectUrl = '';\n  let logoObjectUrl = '';\n  let dragging = null;\n  let redrawQueued = false;\n\n  const clamp = (value, min, max) => Math.min(max, Math.max(min, Number(value) || 0));\n\n  function readState() {\n    try { return JSON.parse(sessionStorage.getItem(STORAGE_KEY) || '{}'); }\n    catch (_) { return {}; }\n  }\n\n  function saveState() {\n    try {\n      sessionStorage.setItem(STORAGE_KEY, JSON.stringify({\n        x: state.x,\n        y: state.y,\n        width: state.width,\n        heightScale: state.heightScale,\n        rotation: state.rotation,\n        opacity: state.opacity,\n        anchor: state.anchor\n      }));\n    } catch (_) {}\n  }\n\n  function toast(message, isError = false) {\n    let element = document.querySelector('.lava360-local-toast');\n    if (!element) {\n      element = document.createElement('div');\n      element.className = 'lava360-local-toast';\n      element.setAttribute('role', 'status');\n      element.setAttribute('aria-live', 'polite');\n      document.body.appendChild(element);\n    }\n    element.textContent = message;\n    element.classList.toggle('is-error', !!isError);\n    element.classList.add('is-show');\n    clearTimeout(element.__timer);\n    element.__timer = setTimeout(() => element.classList.remove('is-show'), isError ? 4800 : 3000);\n  }\n\n  function injectStyles() {\n    if (document.querySelector(`style[data-floor-logo-position=\"${VERSION}\"]`)) return;\n    const style = document.createElement('style');\n    style.dataset.floorLogoPosition = VERSION;\n    style.textContent = `\n      .lava360-floorlogo-launch{margin:0 0 26px;padding:18px 20px;border:1px solid color-mix(in srgb,var(--line,#334155) 70%,transparent);border-radius:18px;background:linear-gradient(135deg,color-mix(in srgb,var(--surface-2,#172033) 96%,transparent),color-mix(in srgb,var(--surface,#111827) 94%,#a855f7 4%));color:var(--text,#f8fafc);display:flex;align-items:center;justify-content:space-between;gap:18px}\n      .lava360-floorlogo-launch h3{margin:0 0 5px;font-size:1.08rem}.lava360-floorlogo-launch p{margin:0;color:var(--muted,#94a3b8);line-height:1.55;font-size:.88rem}.lava360-floorlogo-launch button{flex:0 0 auto;min-height:44px;padding:10px 16px;border:0;border-radius:12px;background:linear-gradient(135deg,#c084fc,#6366f1);color:#10051c;font:900 .88rem/1 Inter,'Noto Sans KR',sans-serif;cursor:pointer}\n      .lava360-floorlogo-modal{position:fixed;inset:0;z-index:11200;display:none;background:rgba(2,6,23,.84);backdrop-filter:blur(9px);padding:12px}.lava360-floorlogo-modal.is-open{display:grid;place-items:center}\n      .lava360-floorlogo-shell{width:min(1500px,100%);height:min(940px,calc(100vh - 24px));display:grid;grid-template-rows:auto minmax(0,1fr) auto;overflow:hidden;border:1px solid rgba(148,163,184,.24);border-radius:22px;background:#08111f;color:#f8fafc;box-shadow:0 34px 110px rgba(0,0,0,.52)}\n      .lava360-floorlogo-head{display:flex;align-items:center;justify-content:space-between;gap:16px;padding:15px 18px;border-bottom:1px solid rgba(148,163,184,.16);background:rgba(8,17,31,.97)}.lava360-floorlogo-head h2{margin:0;font-size:1.12rem}.lava360-floorlogo-head p{margin:3px 0 0;color:#94a3b8;font-size:.8rem}.lava360-floorlogo-head button{width:42px;height:42px;border:0;border-radius:12px;background:#172033;color:#fff;font-size:1.2rem;cursor:pointer}\n      .lava360-floorlogo-body{min-height:0;display:grid;grid-template-columns:286px minmax(0,1fr)}.lava360-floorlogo-panel{min-height:0;overflow:auto;padding:15px;border-right:1px solid rgba(148,163,184,.13);background:#0b1424}.lava360-floorlogo-stage{min-width:0;min-height:0;position:relative;display:grid;place-items:center;overflow:auto;padding:20px;background:repeating-conic-gradient(#0d1728 0 25%,#101d31 0 50%) 50%/28px 28px}\n      .lava360-floorlogo-stage canvas{display:block;max-width:100%;max-height:100%;box-shadow:0 18px 70px rgba(0,0,0,.5);touch-action:none;cursor:grab}.lava360-floorlogo-stage canvas.is-dragging{cursor:grabbing}.lava360-floorlogo-empty{max-width:440px;text-align:center;color:#94a3b8;line-height:1.7}.lava360-floorlogo-empty strong{display:block;margin-bottom:6px;color:#f8fafc;font-size:1.05rem}\n      .lava360-floorlogo-group+.lava360-floorlogo-group{margin-top:18px}.lava360-floorlogo-group h3{margin:0 0 9px;color:#cbd5e1;font-size:.77rem;letter-spacing:.04em}.lava360-floorlogo-field{display:grid;gap:7px;margin-top:11px}.lava360-floorlogo-field label{display:flex;justify-content:space-between;gap:8px;color:#94a3b8;font-size:.76rem;font-weight:800}.lava360-floorlogo-field input[type=range]{width:100%;accent-color:#a855f7}.lava360-floorlogo-field input[type=file],.lava360-floorlogo-field select{width:100%;min-height:41px;border:1px solid #334155;border-radius:10px;background:#111d30;color:#f8fafc;padding:8px 10px;font:inherit}.lava360-floorlogo-file-note{margin-top:7px;color:#7f8ea3;font-size:.72rem;line-height:1.5;word-break:break-all}\n      .lava360-floorlogo-anchor{display:grid;grid-template-columns:repeat(3,34px);gap:5px}.lava360-floorlogo-anchor button{width:34px;height:34px;padding:0;border:1px solid #334155;border-radius:8px;background:#111d30;color:#94a3b8;cursor:pointer}.lava360-floorlogo-anchor button.is-active{border-color:#c084fc;background:rgba(192,132,252,.17);color:#f3e8ff}.lava360-floorlogo-anchor button:before{content:'';display:block;width:7px;height:7px;margin:auto;border-radius:50%;background:currentColor}\n      .lava360-floorlogo-tip{margin-top:10px;padding:10px;border:1px solid #26364e;border-radius:10px;background:#101b2c;color:#8fa0b7;font-size:.73rem;line-height:1.55}\n      .lava360-floorlogo-foot{display:flex;align-items:center;justify-content:space-between;gap:12px;padding:13px 16px;border-top:1px solid rgba(148,163,184,.16);background:#0b1424}.lava360-floorlogo-foot .meta{color:#94a3b8;font-size:.78rem}.lava360-floorlogo-actions{display:flex;gap:9px}.lava360-floorlogo-actions button{min-height:43px;padding:10px 15px;border:1px solid #334155;border-radius:11px;background:#111d30;color:#f8fafc;font:900 .84rem/1 inherit;cursor:pointer}.lava360-floorlogo-actions button.primary{border-color:transparent;background:linear-gradient(135deg,#c084fc,#6366f1);color:#10051c}.lava360-floorlogo-actions button:disabled{opacity:.48;cursor:not-allowed}\n      @media(max-width:820px){.lava360-floorlogo-body{grid-template-columns:230px minmax(0,1fr)}}\n      @media(max-width:700px){.lava360-floorlogo-launch{display:grid}.lava360-floorlogo-launch button{width:100%}.lava360-floorlogo-modal{padding:0}.lava360-floorlogo-shell{height:100dvh;border-radius:0;border:0}.lava360-floorlogo-head{padding:11px 12px}.lava360-floorlogo-head p{display:none}.lava360-floorlogo-body{grid-template-columns:1fr;grid-template-rows:auto minmax(0,1fr)}.lava360-floorlogo-panel{display:grid;grid-template-columns:1fr 1fr;gap:8px;max-height:270px;overflow-y:auto;padding:9px;border-right:0;border-bottom:1px solid rgba(148,163,184,.13)}.lava360-floorlogo-group{min-width:0;margin:0!important}.lava360-floorlogo-group:first-child{grid-column:1/-1}.lava360-floorlogo-group h3{display:none}.lava360-floorlogo-field{margin-top:7px}.lava360-floorlogo-tip{display:none}.lava360-floorlogo-stage{padding:8px;min-height:260px}.lava360-floorlogo-foot{padding:9px;display:grid}.lava360-floorlogo-foot .meta{display:none}.lava360-floorlogo-actions{display:grid;grid-template-columns:1fr 1fr}.lava360-floorlogo-actions button.primary{grid-column:1/-1}}\n    `;\n    document.head.appendChild(style);\n  }\n\n  function inputContext(input) {\n    const parentText = input.closest('label,section,article,div')?.textContent || '';\n    return `${input.name || ''} ${input.id || ''} ${input.accept || ''} ${parentText}`.toLowerCase();\n  }\n\n  function isLogoInput(input) {\n    return /logo|로고|watermark|워터마크|floor|바닥/.test(inputContext(input));\n  }\n\n  function findPanoramaInput() {\n    const inputs = [...document.querySelectorAll('input[type=file]')].filter(input => !input.disabled && (!input.accept || /image/i.test(input.accept)));\n    return inputs.find(input => !isLogoInput(input) && /360|panorama|파노라마|원본|사진|image/.test(inputContext(input))) || inputs.find(input => !isLogoInput(input)) || null;\n  }\n\n  function findLogoInput() {\n    return [...document.querySelectorAll('input[type=file]')].find(input => !input.disabled && isLogoInput(input)) || null;\n  }\n\n  function loadImage(source, crossOrigin = false) {\n    return new Promise((resolve, reject) => {\n      const image = new Image();\n      image.decoding = 'async';\n      if (crossOrigin) image.crossOrigin = 'anonymous';\n      image.onload = () => resolve(image);\n      image.onerror = () => reject(new Error('이미지를 불러오지 못했습니다. 파일을 다시 선택해 주세요.'));\n      image.src = source;\n    });\n  }\n\n  async function sourceFromInput(input) {\n    const file = input?.files?.[0];\n    if (!file?.type?.startsWith('image/')) return null;\n    return { source: URL.createObjectURL(file), file };\n  }\n\n  async function resolvePanorama() {\n    if (panoramaImage) return panoramaImage;\n    const selected = await sourceFromInput(findPanoramaInput());\n    if (selected) {\n      if (panoramaObjectUrl) URL.revokeObjectURL(panoramaObjectUrl);\n      panoramaObjectUrl = selected.source;\n      state.panoramaName = selected.file.name.replace(/\\.[^.]+$/, '') + '-floor-logo.jpg';\n      panoramaImage = await loadImage(selected.source);\n      return panoramaImage;\n    }\n    const source = window.__lava360ViewerConfig?.panorama;\n    if (typeof source === 'string' && source) {\n      panoramaImage = await loadImage(source, !source.startsWith('blob:') && !source.startsWith('data:'));\n      return panoramaImage;\n    }\n    throw new Error('먼저 360 원본 이미지를 불러와 주세요.');\n  }\n\n  async function resolveLogo() {\n    if (logoImage) return logoImage;\n    const selected = await sourceFromInput(findLogoInput());\n    if (!selected) throw new Error('바닥에 넣을 로고 이미지를 선택해 주세요.');\n    if (logoObjectUrl) URL.revokeObjectURL(logoObjectUrl);\n    logoObjectUrl = selected.source;\n    logoImage = await loadImage(selected.source);\n    return logoImage;\n  }\n\n  function anchorOffset(width, height) {\n    const [vertical, horizontal] = state.anchor.split('-');\n    let x = -width / 2;\n    let y = -height / 2;\n    if (horizontal === 'left') x = 0;\n    if (horizontal === 'right') x = -width;\n    if (vertical === 'top') y = 0;\n    if (vertical === 'bottom') y = -height;\n    return { x, y };\n  }\n\n  function logoMetrics(targetWidth, targetHeight) {\n    if (!logoImage) return null;\n    const width = targetWidth * clamp(state.width, 2, 80) / 100;\n    const naturalRatio = (logoImage.naturalHeight || logoImage.height) / Math.max(1, (logoImage.naturalWidth || logoImage.width));\n    const height = width * naturalRatio * clamp(state.heightScale, 10, 160) / 100;\n    return { width, height, x: targetWidth * state.x / 100, y: targetHeight * state.y / 100 };\n  }\n\n  function drawLogo(target, targetWidth, targetHeight, withGuide = false) {\n    if (!logoImage) return;\n    const metrics = logoMetrics(targetWidth, targetHeight);\n    const offset = anchorOffset(metrics.width, metrics.height);\n    const drawAt = x => {\n      target.save();\n      target.globalAlpha = clamp(state.opacity, 0, 100) / 100;\n      target.translate(x, metrics.y);\n      target.rotate(clamp(state.rotation, -180, 180) * Math.PI / 180);\n      target.drawImage(logoImage, offset.x, offset.y, metrics.width, metrics.height);\n      target.restore();\n    };\n    drawAt(metrics.x);\n    const reach = Math.max(metrics.width, metrics.height) * 1.5;\n    if (metrics.x - reach < 0) drawAt(metrics.x + targetWidth);\n    if (metrics.x + reach > targetWidth) drawAt(metrics.x - targetWidth);\n\n    if (withGuide) {\n      target.save();\n      target.translate(metrics.x, metrics.y);\n      target.rotate(clamp(state.rotation, -180, 180) * Math.PI / 180);\n      target.strokeStyle = '#c084fc';\n      target.lineWidth = Math.max(1, targetWidth / 900);\n      target.setLineDash([targetWidth / 280, targetWidth / 360]);\n      target.strokeRect(offset.x, offset.y, metrics.width, metrics.height);\n      target.setLineDash([]);\n      target.fillStyle = '#08111f';\n      target.strokeStyle = '#e9d5ff';\n      target.beginPath();\n      target.arc(0, 0, Math.max(4, targetWidth / 260), 0, Math.PI * 2);\n      target.fill();\n      target.stroke();\n      target.restore();\n    }\n  }\n\n  function queueRender() {\n    if (redrawQueued) return;\n    redrawQueued = true;\n    requestAnimationFrame(() => { redrawQueued = false; render(); });\n  }\n\n  function render() {\n    if (!canvas || !ctx || !panoramaImage) return;\n    ctx.clearRect(0, 0, canvas.width, canvas.height);\n    ctx.drawImage(panoramaImage, 0, 0, canvas.width, canvas.height);\n    drawLogo(ctx, canvas.width, canvas.height, true);\n    refreshUI();\n  }\n\n  function setupCanvas() {\n    const stage = modal.querySelector('.lava360-floorlogo-stage');\n    stage.innerHTML = '';\n    canvas = document.createElement('canvas');\n    const scale = Math.min(1, MAX_PREVIEW_WIDTH / panoramaImage.naturalWidth);\n    canvas.width = Math.max(2, Math.round(panoramaImage.naturalWidth * scale));\n    canvas.height = Math.max(1, Math.round(panoramaImage.naturalHeight * scale));\n    ctx = canvas.getContext('2d');\n    stage.appendChild(canvas);\n    bindCanvas();\n    render();\n  }\n\n  function pointFromEvent(event) {\n    const rect = canvas.getBoundingClientRect();\n    return {\n      x: clamp((event.clientX - rect.left) / rect.width * 100, 0, 100),\n      y: clamp((event.clientY - rect.top) / rect.height * 100, 0, 100)\n    };\n  }\n\n  function bindCanvas() {\n    canvas.addEventListener('pointerdown', event => {\n      const point = pointFromEvent(event);\n      const metrics = logoMetrics(canvas.width, canvas.height);\n      const dx = ((point.x - state.x) / 100) * canvas.width;\n      const dy = ((point.y - state.y) / 100) * canvas.height;\n      const radius = Math.max(metrics?.width || 0, metrics?.height || 0) * .75 + 24;\n      if (!logoImage || Math.hypot(dx, dy) > radius) return;\n      dragging = { pointerId: event.pointerId };\n      canvas.setPointerCapture?.(event.pointerId);\n      canvas.classList.add('is-dragging');\n      event.preventDefault();\n    });\n    canvas.addEventListener('pointermove', event => {\n      if (!dragging) return;\n      const point = pointFromEvent(event);\n      state.x = point.x;\n      state.y = point.y;\n      saveState();\n      queueRender();\n    });\n    const finish = () => {\n      if (!dragging) return;\n      dragging = null;\n      canvas.classList.remove('is-dragging');\n      saveState();\n      emitPosition();\n    };\n    canvas.addEventListener('pointerup', finish);\n    canvas.addEventListener('pointercancel', finish);\n  }\n\n  function createModal() {\n    if (modal) return;\n    modal = document.createElement('div');\n    modal.className = 'lava360-floorlogo-modal';\n    modal.setAttribute('role', 'dialog');\n    modal.setAttribute('aria-modal', 'true');\n    modal.setAttribute('aria-label', '바닥 로고 위치 편집기');\n    const anchors = [\n      ['top-left','좌측 위'],['top-center','가운데 위'],['top-right','우측 위'],\n      ['center-left','좌측 가운데'],['center','가운데'],['center-right','우측 가운데'],\n      ['bottom-left','좌측 아래'],['bottom-center','가운데 아래'],['bottom-right','우측 아래']\n    ];\n    modal.innerHTML = `\n      <div class=\"lava360-floorlogo-shell\">\n        <header class=\"lava360-floorlogo-head\"><div><h2>바닥 로고 위치·시작점 편집</h2><p>펼친 360 원본에서 로고를 직접 드래그하고, 기준점·회전·크기·투명도를 조절하세요.</p></div><button type=\"button\" data-floorlogo-close aria-label=\"닫기\">×</button></header>\n        <div class=\"lava360-floorlogo-body\">\n          <aside class=\"lava360-floorlogo-panel\">\n            <section class=\"lava360-floorlogo-group\"><h3>로고 파일</h3><div class=\"lava360-floorlogo-field\"><input type=\"file\" accept=\"image/png,image/jpeg,image/webp,image/svg+xml\" data-floorlogo-file></div><div class=\"lava360-floorlogo-file-note\" data-floorlogo-file-note>기존 편집기에서 선택한 로고를 자동으로 가져옵니다.</div></section>\n            <section class=\"lava360-floorlogo-group\"><h3>위치와 시작점</h3>\n              <div class=\"lava360-floorlogo-field\"><label>가로 위치 X <output data-floorlogo-x-value></output></label><input type=\"range\" min=\"0\" max=\"100\" step=\"0.1\" data-floorlogo-x></div>\n              <div class=\"lava360-floorlogo-field\"><label>세로 위치 Y <output data-floorlogo-y-value></output></label><input type=\"range\" min=\"45\" max=\"100\" step=\"0.1\" data-floorlogo-y></div>\n              <div class=\"lava360-floorlogo-field\"><label>로고 기준점</label><div class=\"lava360-floorlogo-anchor\">${anchors.map(([key,label]) => `<button type=\"button\" data-floorlogo-anchor=\"${key}\" aria-label=\"${label}\" title=\"${label}\"></button>`).join('')}</div></div>\n            </section>\n            <section class=\"lava360-floorlogo-group\"><h3>크기와 모양</h3>\n              <div class=\"lava360-floorlogo-field\"><label>크기 <output data-floorlogo-width-value></output></label><input type=\"range\" min=\"3\" max=\"55\" step=\"0.5\" data-floorlogo-width></div>\n              <div class=\"lava360-floorlogo-field\"><label>바닥 원근·높이 <output data-floorlogo-height-value></output></label><input type=\"range\" min=\"20\" max=\"130\" step=\"1\" data-floorlogo-height></div>\n              <div class=\"lava360-floorlogo-field\"><label>회전 <output data-floorlogo-rotation-value></output></label><input type=\"range\" min=\"-180\" max=\"180\" step=\"1\" data-floorlogo-rotation></div>\n              <div class=\"lava360-floorlogo-field\"><label>투명도 <output data-floorlogo-opacity-value></output></label><input type=\"range\" min=\"5\" max=\"100\" step=\"1\" data-floorlogo-opacity></div>\n              <div class=\"lava360-floorlogo-tip\">보라색 점이 로고의 시작점입니다. 로고를 직접 드래그하거나 X·Y 슬라이더를 사용하세요. 좌우 끝을 넘어가도 360 경계에서 자연스럽게 이어집니다.</div>\n            </section>\n          </aside>\n          <main class=\"lava360-floorlogo-stage\"><div class=\"lava360-floorlogo-empty\"><strong>360 이미지와 로고를 준비하고 있습니다.</strong>기존 편집기에서 선택한 파일을 자동으로 불러옵니다.</div></main>\n        </div>\n        <footer class=\"lava360-floorlogo-foot\"><div class=\"meta\" data-floorlogo-meta>로고 연결 대기</div><div class=\"lava360-floorlogo-actions\"><button type=\"button\" data-floorlogo-action=\"reset\">기본 위치</button><button type=\"button\" data-floorlogo-action=\"download\">로고 적용본 저장</button><button type=\"button\" class=\"primary\" data-floorlogo-action=\"apply\">360 편집기에 적용</button></div></footer>\n      </div>`;\n    document.body.appendChild(modal);\n    bindModal();\n  }\n\n  function bindModal() {\n    modal.querySelector('[data-floorlogo-close]').addEventListener('click', closeEditor);\n    modal.addEventListener('click', event => { if (event.target === modal) closeEditor(); });\n    modal.querySelector('[data-floorlogo-file]').addEventListener('change', async event => {\n      const file = event.target.files?.[0];\n      if (!file?.type?.startsWith('image/')) return;\n      if (logoObjectUrl) URL.revokeObjectURL(logoObjectUrl);\n      logoObjectUrl = URL.createObjectURL(file);\n      try {\n        logoImage = await loadImage(logoObjectUrl);\n        modal.querySelector('[data-floorlogo-file-note]').textContent = file.name;\n        render();\n      } catch (error) { toast(error.message, true); }\n    });\n    const bindRange = (selector, key, min, max) => {\n      modal.querySelector(selector).addEventListener('input', event => {\n        state[key] = clamp(event.target.value, min, max);\n        saveState();\n        queueRender();\n      });\n      modal.querySelector(selector).addEventListener('change', emitPosition);\n    };\n    bindRange('[data-floorlogo-x]', 'x', 0, 100);\n    bindRange('[data-floorlogo-y]', 'y', 0, 100);\n    bindRange('[data-floorlogo-width]', 'width', 3, 55);\n    bindRange('[data-floorlogo-height]', 'heightScale', 20, 130);\n    bindRange('[data-floorlogo-rotation]', 'rotation', -180, 180);\n    bindRange('[data-floorlogo-opacity]', 'opacity', 5, 100);\n    modal.querySelectorAll('[data-floorlogo-anchor]').forEach(button => button.addEventListener('click', () => {\n      state.anchor = button.dataset.floorlogoAnchor;\n      saveState();\n      render();\n      emitPosition();\n    }));\n    modal.querySelectorAll('[data-floorlogo-action]').forEach(button => button.addEventListener('click', () => handleAction(button.dataset.floorlogoAction)));\n    document.addEventListener('keydown', event => {\n      if (!modal?.classList.contains('is-open')) return;\n      if (event.key === 'Escape') closeEditor();\n      if (event.key.startsWith('Arrow') && !/INPUT|SELECT/.test(document.activeElement?.tagName || '')) {\n        event.preventDefault();\n        const step = event.shiftKey ? 1 : .2;\n        if (event.key === 'ArrowLeft') state.x = clamp(state.x - step, 0, 100);\n        if (event.key === 'ArrowRight') state.x = clamp(state.x + step, 0, 100);\n        if (event.key === 'ArrowUp') state.y = clamp(state.y - step, 0, 100);\n        if (event.key === 'ArrowDown') state.y = clamp(state.y + step, 0, 100);\n        saveState(); render();\n      }\n    });\n  }\n\n  function refreshUI() {\n    if (!modal) return;\n    const values = {\n      '[data-floorlogo-x]': state.x,\n      '[data-floorlogo-y]': state.y,\n      '[data-floorlogo-width]': state.width,\n      '[data-floorlogo-height]': state.heightScale,\n      '[data-floorlogo-rotation]': state.rotation,\n      '[data-floorlogo-opacity]': state.opacity\n    };\n    Object.entries(values).forEach(([selector,value]) => { const input = modal.querySelector(selector); if (input) input.value = value; });\n    const texts = {\n      '[data-floorlogo-x-value]': `${Number(state.x).toFixed(1)}%`,\n      '[data-floorlogo-y-value]': `${Number(state.y).toFixed(1)}%`,\n      '[data-floorlogo-width-value]': `${Number(state.width).toFixed(1)}%`,\n      '[data-floorlogo-height-value]': `${Math.round(state.heightScale)}%`,\n      '[data-floorlogo-rotation-value]': `${Math.round(state.rotation)}°`,\n      '[data-floorlogo-opacity-value]': `${Math.round(state.opacity)}%`\n    };\n    Object.entries(texts).forEach(([selector,value]) => { const output = modal.querySelector(selector); if (output) output.textContent = value; });\n    modal.querySelectorAll('[data-floorlogo-anchor]').forEach(button => button.classList.toggle('is-active', button.dataset.floorlogoAnchor === state.anchor));\n    const meta = modal.querySelector('[data-floorlogo-meta]');\n    if (meta) meta.textContent = panoramaImage && logoImage ? `${panoramaImage.naturalWidth}×${panoramaImage.naturalHeight}px · 시작점 ${state.x.toFixed(1)}%, ${state.y.toFixed(1)}%` : '로고 연결 대기';\n    modal.querySelectorAll('[data-floorlogo-action=\"apply\"],[data-floorlogo-action=\"download\"]').forEach(button => { button.disabled = !(panoramaImage && logoImage); });\n  }\n\n  async function openEditor() {\n    createModal();\n    modal.classList.add('is-open');\n    document.documentElement.style.overflow = 'hidden';\n    try {\n      await Promise.all([resolvePanorama(), resolveLogo().catch(() => null)]);\n      const existing = findLogoInput()?.files?.[0];\n      if (existing) modal.querySelector('[data-floorlogo-file-note]').textContent = existing.name;\n      setupCanvas();\n      if (!logoImage) toast('바닥 로고 파일을 선택해 주세요.', true);\n    } catch (error) {\n      const empty = modal.querySelector('.lava360-floorlogo-empty');\n      if (empty) empty.innerHTML = `<strong>이미지가 필요합니다.</strong>${error.message}`;\n      toast(error.message, true);\n    }\n    refreshUI();\n  }\n\n  function closeEditor() {\n    modal?.classList.remove('is-open');\n    document.documentElement.style.overflow = '';\n  }\n\n  function resetPosition() {\n    Object.assign(state, { x: 50, y: 82, width: 18, heightScale: 72, rotation: 0, opacity: 100, anchor: 'center' });\n    saveState();\n    render();\n    emitPosition();\n    toast('바닥 로고 위치를 기본값으로 초기화했습니다.');\n  }\n\n  function emitPosition() {\n    window.dispatchEvent(new CustomEvent('lava360:floorlogo-position', { detail: {\n      x: state.x, y: state.y, width: state.width, heightScale: state.heightScale,\n      rotation: state.rotation, opacity: state.opacity, anchor: state.anchor\n    } }));\n  }\n\n  async function handleAction(action) {\n    if (action === 'reset') return resetPosition();\n    if (action === 'download') return output(false);\n    if (action === 'apply') return output(true);\n  }\n\n  async function output(apply) {\n    if (!panoramaImage || !logoImage) return toast('360 이미지와 로고를 모두 준비해 주세요.', true);\n    const button = modal.querySelector(`[data-floorlogo-action=\"${apply ? 'apply' : 'download'}\"]`);\n    const original = button.textContent;\n    try {\n      button.disabled = true;\n      button.textContent = '고해상도 처리 중…';\n      const out = document.createElement('canvas');\n      out.width = panoramaImage.naturalWidth;\n      out.height = panoramaImage.naturalHeight;\n      const outCtx = out.getContext('2d');\n      outCtx.drawImage(panoramaImage, 0, 0, out.width, out.height);\n      drawLogo(outCtx, out.width, out.height, false);\n      const blob = await new Promise((resolve, reject) => out.toBlob(value => value ? resolve(value) : reject(new Error('바닥 로고 이미지를 만들지 못했습니다.')), 'image/jpeg', .95));\n      if (!apply) {\n        downloadBlob(blob, state.panoramaName);\n        toast('바닥 로고가 적용된 360 이미지를 저장했습니다.');\n        return;\n      }\n      const file = new File([blob], state.panoramaName, { type: 'image/jpeg', lastModified: Date.now() });\n      const input = findPanoramaInput();\n      if (input && typeof DataTransfer !== 'undefined') {\n        const transfer = new DataTransfer();\n        transfer.items.add(file);\n        input.files = transfer.files;\n        input.dispatchEvent(new Event('change', { bubbles: true }));\n      } else {\n        const url = URL.createObjectURL(blob);\n        window.dispatchEvent(new CustomEvent('lava360:floorlogosource', { detail: { url, file } }));\n      }\n      closeEditor();\n      toast('바닥 로고 위치를 적용했습니다. 정면·수평·모자이크·2D 출력 작업을 계속할 수 있습니다.');\n    } catch (error) {\n      toast(error.message || '바닥 로고 처리에 실패했습니다.', true);\n    } finally {\n      button.disabled = false;\n      button.textContent = original;\n    }\n  }\n\n  function downloadBlob(blob, filename) {\n    const url = URL.createObjectURL(blob);\n    const anchor = document.createElement('a');\n    anchor.href = url;\n    anchor.download = filename;\n    anchor.style.display = 'none';\n    document.body.appendChild(anchor);\n    anchor.click();\n    anchor.remove();\n    setTimeout(() => URL.revokeObjectURL(url), 30000);\n  }\n\n  function createLauncher() {\n    if (panel || document.querySelector('.lava360-floorlogo-launch')) return;\n    const mount = document.querySelector('.lava360-output-tools') || document.querySelector('#editor .container') || document.querySelector('#main .container') || document.querySelector('#main');\n    if (!mount) return;\n    panel = document.createElement('section');\n    panel.className = 'lava360-floorlogo-launch';\n    panel.innerHTML = `<div><h3>바닥 로고 위치·시작점</h3><p>고정된 중앙 위치가 아니라 360 원본 위에서 로고를 직접 드래그하고, 기준점·회전·원근·투명도를 조절합니다.</p></div><button type=\"button\" data-open-floorlogo>바닥 로고 위치 편집</button>`;\n    mount.appendChild(panel);\n    panel.querySelector('[data-open-floorlogo]').addEventListener('click', openEditor);\n  }\n\n  function rememberFiles(event) {\n    const input = event.target;\n    if (!(input instanceof HTMLInputElement) || input.type !== 'file' || !input.files?.length) return;\n    const file = [...input.files].find(item => item.type.startsWith('image/'));\n    if (!file) return;\n    if (isLogoInput(input)) {\n      if (logoObjectUrl) URL.revokeObjectURL(logoObjectUrl);\n      logoObjectUrl = URL.createObjectURL(file);\n      loadImage(logoObjectUrl).then(image => { logoImage = image; render(); }).catch(() => {});\n    } else {\n      if (panoramaObjectUrl) URL.revokeObjectURL(panoramaObjectUrl);\n      panoramaObjectUrl = URL.createObjectURL(file);\n      state.panoramaName = file.name.replace(/\\.[^.]+$/, '') + '-floor-logo.jpg';\n      loadImage(panoramaObjectUrl).then(image => { panoramaImage = image; if (modal?.classList.contains('is-open')) setupCanvas(); }).catch(() => {});\n    }\n  }\n\n  function interceptLegacyButtons() {\n    document.addEventListener('click', event => {\n      const button = event.target.closest('button');\n      if (!button || button.closest('.lava360-floorlogo-modal') || button.matches('[data-open-floorlogo]')) return;\n      const text = (button.textContent || '').replace(/\\s+/g, ' ').trim();\n      if (/바닥.*로고.*(편집|위치|배치)|로고.*바닥.*(편집|위치|배치)|floor.*logo|nadir.*logo/i.test(text)) {\n        event.preventDefault();\n        event.stopImmediatePropagation();\n        openEditor();\n      }\n    }, true);\n  }\n\n  injectStyles();\n  createModal();\n  createLauncher();\n  interceptLegacyButtons();\n  document.addEventListener('change', rememberFiles, true);\n  const observer = new MutationObserver(createLauncher);\n  observer.observe(document.documentElement, { childList: true, subtree: true });\n})();\n";
